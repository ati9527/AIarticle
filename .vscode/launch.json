{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Local HTML",
            "request": "launch",
            "type": "chrome",
            "file": "${workspaceFolder}/index.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
